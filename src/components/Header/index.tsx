import { HeaderContainer, HeaderContainerTitle } from './styled';

export const Header = () => {
  return (
    <>
      <HeaderContainer>
        <HeaderContainerTitle>VITE</HeaderContainerTitle>
      </HeaderContainer>
    </>
  );
};
