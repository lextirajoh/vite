import styled from 'styled-components';

export const HeaderContainer = styled.nav`
  width: 100%;
  padding: 0 8px;
  color: var(--black);
  background-color: var(--gold);
`;

export const HeaderContainerTitle = styled.h1`
  font-size: 2rem;
`;
